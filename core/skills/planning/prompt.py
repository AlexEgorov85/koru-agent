"""
Промты для навыка планирования с поддержкой архитектуры портов и адаптеров.
ОСОБЕННОСТИ РЕАЛИЗАЦИИ:
- Убрано явное указание формата вывода
- Добавлены плейсхолдеры для списка доступных capability
- Упрощена структура промптов
- Улучшена поддержка различных стратегий планирования
АРХИТЕКТУРНЫЕ ПРИНЦИПЫ:
- Промты фокусируются на содержании, а не на формате вывода
- ExecutionGateway обрабатывает форматирование через output_schema
- Capability передаются в промпты для информирования LLM о возможностях системы
- Безопасность приоритетна над функциональностью
"""

CREATE_PLAN_PROMPT = """
Ты — модуль планирования агентной системы.
Твоя задача — создать ПЕРВИЧНЫЙ план действий для достижения цели.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ИНСТРУКЦИИ:
1. СТРОЙ план с нуля на основе цели
2. ДЕЛИ план на конкретные, выполнимые шаги
3. УЧИТЫВАЙ доступные возможности системы при выборе действий
4. ДЕЛАЙ шаги последовательными и логичными
5. УКАЖИ реалистичные оценки времени для каждого шага
6. УЧИТЫВАЙ ограничения системы (максимум {max_steps} шагов)

ЦЕЛЬ:
{goal}

ДОПОЛНИТЕЛЬНЫЙ КОНТЕКСТ:
{context}
"""

UPDATE_PLAN_PROMPT = """
Ты — модуль обновления плана агентной системы.
Твоя задача — ОБНОВИТЬ существующий план на основе прогресса или ошибок.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ИНСТРУКЦИИ:
1. НЕ пересобирай план с нуля, сохраняй существующую структуру
2. УЧИТЫВАЙ уже выполненные шаги (статус "completed")
3. ИЗМЕНЯЙ только необходимые шаги
4. ДОБАВЛЯЙ новые шаги только при явной необходимости
5. УДАЛЯЙ шаги только если они стали неактуальны
6. СОХРАНЯЙ связность плана

ТЕКУЩИЙ ПЛАН:
{current_plan}

ОБНОВЛЕНИЯ ДЛЯ ПРИМЕНЕНИЯ:
{updates}

ДОПОЛНИТЕЛЬНЫЙ КОНТЕКСТ:
{context}
"""

DECOMPOSE_TASK_PROMPT = """
Ты — модуль декомпозиции задач агентной системы.
Твоя задача — разбить сложную задачу на управляемые подзадачи.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ИНСТРУКЦИИ:
1. АНАЛИЗИРУЙ сложность исходной задачи
2. ОПРЕДЕЛИ критерии для декомпозиции
3. СОЗДАЙ логически связанные подзадачи
4. УБЕДИСЬ, что подзадачи независимы и выполнимы
5. СОХРАНЯЙ иерархическую структуру
6. УЧИТЫВАЙ доступные возможности системы при формировании подзадач

ID ЗАДАЧИ: {task_id}
ОПИСАНИЕ ЗАДАЧИ: {task_description}
ДОПОЛНИТЕЛЬНЫЙ КОНТЕКСТ:
{context}
"""

MARK_TASK_COMPLETED_PROMPT = """
Ты — модуль отслеживания прогресса агентной системы.
Твоя задача — зафиксировать завершение задачи и проанализировать результат.

ID ЗАДАЧИ: {task_id}
РЕЗУЛЬТАТ ВЫПОЛНЕНИЯ: {result_summary}
ОЦЕНКА КАЧЕСТВА: {quality_score}
ВРЕМЯ ВЫПОЛНЕНИЯ (МИН): {time_spent_minutes}

ИНСТРУКЦИИ:
1. ПОДТВЕРДИ факт завершения задачи
2. ПРОАНАЛИЗИРУЙ результат выполнения
3. ОЦЕНИ качество выполнения (0.0-1.0)
4. УКАЖИ следующие рекомендуемые действия
5. ОБНОВИ оценку времени для связанных задач
6. ОПРЕДЕЛИ необходимость корректировки плана
"""

UPDATE_STEP_STATUS_PROMPT = """Ты — модуль планирования агентной системы.
Твоя задача — проанализировать влияние обновления статуса шага на общий план и предложить корректировки при необходимости.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ТЕКУЩИЙ ПЛАН:
{current_plan}

ОБНОВЛЕНИЕ:
- Шаг ID: {step_id}
- План ID: {plan_id}
- Новый статус: {status}

КОНТЕКСТ:
{context}

ИНСТРУКЦИИ:
1. ПРОАНАЛИЗИРУЙ влияние обновления статуса шага на общий план
2. ОПРЕДЕЛИ, требуются ли корректировки других шагов
3. ПРЕДЛОЖИ конкретные корректировки при необходимости:
   - Изменение статуса зависимых шагов
   - Обновление описания шагов
   - Корректировка оценки времени
4. УКАЖИ следующий рекомендуемый шаг для выполнения
5. ДАЙ краткую оценку влияния на общий план

ВАЖНО:
- Если статус "failed", проанализируй возможные причины и предложи альтернативы
- Если статус "completed", проверь, можно ли выполнить зависимые шаги
- Учитывай реальные возможности системы при предложениях корректировок
- Будь конкретен в рекомендациях
"""

GET_NEXT_STEP_PROMPT = """Ты — модуль планирования агентной системы.
Твоя задача — проанализировать следующий шаг в плане и подготовить рекомендации для его выполнения.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ТЕКУЩИЙ ШАГ:
{current_step}

КОНТЕКСТ:
{context}

ИНСТРУКЦИИ:
1. ПРОАНАЛИЗИРУЙ текущий шаг и его зависимости
2. ПРОВЕРЬ, выполнены ли все зависимости для этого шага
3. ОЦЕНИ возможность выполнения шага с текущими возможностями системы
4. ПРЕДЛОЖИ конкретные рекомендации для успешного выполнения
5. ОЦЕНИ реалистичное время выполнения в минутах
6. УКАЖИ уровень уверенности в успешном выполнении (0.0-1.0)

ВАЖНО:
- Если зависимости не выполнены, укажи это явно
- Если шаг требует capability, которых нет в системе, предложи альтернативу
- Учитывай контекст выполнения предыдущих шагов
- Будь реалистичен в оценках времени и уверенности
"""

UPDATE_STEP_STATUS_PROMPT = """Ты — модуль планирования агентной системы.
Твоя задача — проанализировать влияние обновления статуса шага на общий план и предложить корректировки при необходимости.

ДОСТУПНЫЕ ВОЗМОЖНОСТИ СИСТЕМЫ:
{capabilities_list}

ТЕКУЩИЙ ПЛАН:
{current_plan}

ОБНОВЛЕНИЕ:
- Шаг ID: {step_id}
- План ID: {plan_id}
- Новый статус: {status}

КОНТЕКСТ:
{context}

ИНСТРУКЦИИ:
1. ПРОАНАЛИЗИРУЙ влияние обновления статуса шага на общий план
2. ОПРЕДЕЛИ, требуются ли корректировки других шагов
3. ПРЕДЛОЖИ конкретные корректировки при необходимости:
   - Изменение статуса зависимых шагов
   - Обновление описания шагов
   - Корректировка оценки времени
4. УКАЖИ следующий рекомендуемый шаг для выполнения
5. ДАЙ краткую оценку влияния на общий план

ВАЖНО:
- Если статус "failed", проанализируй возможные причины и предложи альтернативы
- Если статус "completed", проверь, можно ли выполнить зависимые шаги
- Учитывай реальные возможности системы при предложениях корректировок
- Будь конкретен в рекомендациях
"""
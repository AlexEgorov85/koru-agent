"""
Промпты для операций поиска (опционально, используется только для сложных сценариев).
ПРИМЕЧАНИЕ: Базовый поиск реализован через структурный анализ без LLM.
Для сложных семантических запросов можно раскомментировать и использовать.
"""
# Базовый поиск реализован без промптов через структурный анализ ProjectMap
# Промпты остаются для будущего расширения функционала

SEARCH_PROMPT_TEMPLATE = """
Ты — эксперт по кодовой базе. Проанализируй запрос и найди релевантные элементы.

ЗАПРОС ПОЛЬЗОВАТЕЛЯ:
{query}

ДОСТУПНЫЕ ЭЛЕМЕНТЫ:
{available_elements}

ИНСТРУКЦИИ:
1. Найди элементы, наиболее релевантные запросу
2. Учти тип элемента (класс, функция, метод)
3. Оцени релевантность по шкале 0.0-1.0
4. Верни ТОЛЬКО валидный JSON без дополнительного текста

ФОРМАТ ВЫВОДА:
{
  "results": [
    {
      "name": "имя_элемента",
      "file_path": "путь/к/файлу",
      "type": "class|function|method",
      "line": 45,
      "relevance_score": 0.95,
      "context": "краткий контекст"
    }
  ]
}
"""
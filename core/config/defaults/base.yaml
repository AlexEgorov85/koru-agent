# config/base.yaml
# Базовая конфигурация для всех профилей
log_level: INFO
log_dir: logs
data_dir: data
agent:
  max_steps: 10
  max_retries: 3
  temperature: 0.7
security:
  secrets_path: config/secrets.yaml
providers:
  tools:
    SQLTool:
      enabled: true
# 🗺️ ROADMAP: Composable AI Agent Framework

> Последнее обновление: 06.02.2026  
> Статус проекта: **Продвинутый прототип** — архитектура готова, реализация требует завершения ключевых компонентов  
> Стратегия: Документация описывает цель → этот файл — путь к ней. Не переписываем документацию, а закрываем разрыв через реализацию.

---

## 🔴 КРИТИЧЕСКИЕ ЗАДАЧИ (Блокируют работу на Windows / нарушают инварианты)

| ✅ | Задача | Критерий приемки | Срок |
|----|--------|------------------|------|
| ☐ | **Исправить путь проекта**<br>`C:\Users\Алексей\...\Agent_code ` → убрать пробел в конце | 1. Автотест `test_path_validation_with_trailing_space` проходит<br>2. `FileListerTool` работает без ошибок на реальном проекте | 1 день |
| ☐ | **Починить валидацию ProjectStructure**<br>`len(files) != total_files` | 1. `ProjectMapSkill._validate_structure()` не возвращает ошибку `Несоответствие между количеством файлов и total_files`<br>2. Тест `test_project_structure_validation` проходит на 10+ реальных проектах | 2 дня |
| ☐ | **Добавить индексацию в CodeUnit** | 1. `ProjectStructure.get_code_units_by_name("main")` работает за O(1)<br>2. `ProjectStructure.get_code_units_by_type("class")` возвращает результат без полного сканирования | 3 дня |
| ☐ | **Заполнить зависимости в FileInfo** | После анализа `file_info.imports` и `file_info.dependencies` содержат данные (не пустые списки) | 2 дня |

---

## 🟠 ЯДРО АГЕНТА (Без этого нет рабочего агента)

| ✅ | Задача | Критерий приемки | Срок |
|----|--------|------------------|------|
| ☐ | **Реализовать `AgentRuntime.execute_task()`**<br>Заменить заглушку на рабочую логику | 1. Агент выполняет 3+ итерации цикла THINK → ACT → OBSERVE<br>2. Пример `examples/simple_agent_demo.py` работает без `NotImplementedError` | 5 дней |
| ☐ | **Убрать заглушку в `CodeAnalysisSkill`**<br>«Заглушка для анализа качества кода» → реальный анализ | 1. Возвращает метрики: `complexity`, `duplication_risk`, `style_violations`<br>2. Работает на файлах >1000 строк без падений | 3 дня |
| ☐ | **Интегрировать агент с навыками через `Capability`** | 1. Вызов `skill.execute()` происходит из реального потока выполнения агента (не через моки)<br>2. Тест `test_agent_skill_integration` проходит | 2 дня |
| ☐ | **Реализовать `TypeScriptParser.get_outline()`** | 1. Парсинг `.ts` файлов без исключений `NotImplementedError`<br>2. Возвращает структуру: функции, классы, интерфейсы | 4 дня |

---

## 🟡 ПРОДАКШН-ГОТОВНОСТЬ (Безопасность, мониторинг, надёжность)

| ✅ | Задача | Критерий приемки | Срок |
|----|--------|------------------|------|
| ☐ | **Валидация генерируемого кода** | 1. Тест на генерацию `os.system("rm -rf /")` → отклоняется с ошибкой `SECURITY_VIOLATION`<br>2. Белый список разрешённых паттернов (чтение файлов, анализ кода) | 3 дня |
| ☐ | **Реализовать `PromptRepository.update_usage_metrics()`** | 1. После вызова промта `usage_metrics.success_count` увеличивается<br>2. Метрики сохраняются между запусками (не только in-memory) | 2 дня |
| ☐ | **Файловое хранение снапшотов**<br>Заменить `FileSnapshotManager` (in-memory) на файловое | 1. Снапшоты сохраняются в `data/snapshots/`<br>2. После перезапуска агента можно загрузить историю выполнения | 2 дня |
| ☐ | **Исправить тесты под философию проекта** | 1. В `tests/unit/` нет моков внутренних компонентов (только границы: LLM, DB)<br>2. Все тесты проходят с флагом `--strict-markers` | 3 дня |

---

## 🟢 ОПТИМИЗАЦИЯ И РАСШИРЕНИЕ

| ✅ | Задача | Критерий приемки |
|----|--------|------------------|
| ☐ | **Кэширование AST** | Повторный анализ файла происходит в 5x быстрее (кэш по хешу содержимого) |
| ☐ | **Стратегия сжатия контекста** | Агент работает с проектами >500 файлов без превышения лимита токенов |
| ☐ | **Полная поддержка TypeScript** | Парсинг дженериков, декораторов, пространств имён |
| ☐ | **Интеграция бенчмарков с реальным агентом** | `BenchmarkRunner` вызывает `agent.execute_task()`, а не моки |

---

## 📅 ПЛАН ИТЕРАЦИЙ

### 🚀 Итерация 1 (1 неделя): Стабилизация ядра
```text
[ ] Исправить путь с пробелом (блокер для Windows)
[ ] Починить валидацию ProjectStructure
[ ] Добавить индексацию в CodeUnit
[ ] Заполнить зависимости в FileInfo
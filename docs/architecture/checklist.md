# 📋 Чек-лист архитектурной зрелости системы агентов
*Заполняйте каждые 2 недели. Тренды помогут отслеживать прогресс.*

---

## 🔑 Легенда
| Символ | Значение |
|--------|----------|
| ✅ | Соответствует архитектуре |
| ⚠️ | Частично соответствует / требует улучшения |
| ❌ | Нарушает архитектурные принципы |
| 🚧 | В процессе исправления |
| 📈 | Тренд улучшения |
| 📉 | Тренд ухудшения |
| ➡️ | Стабильно |

---

## 🧱 Блок 1: Разделение слоёв (Инфраструктура → Приложение → Сессия)

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 1.1 | `InfrastructureContext` содержит ТОЛЬКО тяжёлые ресурсы | Провайдеры (LLM/DB), хранилища (без кэша), шина событий. Нет `PromptService`/`ContractService` с кэшами | ✅ `grep -r "class PromptService" core/infrastructure/` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Сервисы с кэшами регистрируются в инфраструктуре |
| 1.2 | `ApplicationContext` создаёт изолированные экземпляры сервисов | `id(ctx1.prompt_service) != id(ctx2.prompt_service)` для двух контекстов | ✅ Тест `test_cache_isolation.py` | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Общий кэш промптов между агентами |
| 1.3 | Инструменты (`BaseTool`) stateless | Нет атрибутов `_cache`, `_state`, `_history` в инструментах | ✅ `grep -r "self\._.*cache\|self\._.*state" core/application/tools/` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | `SQLTool`, `FileTool` без состояния |
| 1.4 | Навыки (`BaseSkill`) используют изолированные кэши | `id(skill1._cached_prompts) != id(skill2._cached_prompts)` | ✅ Тест `test_skill_isolation.py` | 🔴 Критический | ⚠️ | ➡️ | 13.02.2026 | Кэши есть, но могут быть общими из-за сервисов |
| 1.5 | Сессионный контекст (`SessionContext`) append-only | Невозможно изменить историю после добавления шага | ✅ Тест `test_append_only_semantics.py` | 🟢 Желательный | ✅ | 📈 | 13.02.2026 | Реализовано корректно |
| 1.6 | Чёткие границы зависимостей | Прикладной слой → только чтение из инфраструктуры. Нет `infra.register_resource()` из `app_ctx` | ✅ Статический анализ зависимостей | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Границы соблюдены |

---

## 🔐 Блок 2: Безопасность и изоляция

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 2.1 | Изоляция кэшей между агентами | 2 агента с разными версиями → разные промпты/контракты | ✅ Тест `test_cache_isolation.py` | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Общий кэш из-за регистрации сервисов в инфраструктуре |
| 2.2 | Общность провайдеров между агентами | `id(ctx1.infra.llm) == id(ctx2.infra.llm)` | ✅ Тест `test_shared_providers.py` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Провайдеры общие — правильно |
| 2.3 | Защита от раннего доступа к кэшам | Доступ к `get_prompt()` до `initialize()` → `RuntimeError` | ✅ Тест `test_early_access_protection.py` | 🟠 Важный | ⚠️ | ➡️ | 13.02.2026 | Частично реализовано в `BaseComponent` |
| 2.4 | Path traversal защита в `FileTool` | Попытка `../../etc/passwd` → `SecurityError` | ✅ Тест `test_filetool_security.py` | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Реализовано через `resolve()` + валидация |
| 2.5 | Изоляция `base_path` между агентами | Агент А не видит файлы Агента Б | ✅ Тест `test_filetool_isolation.py` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Реализовано через `ComponentConfig` |
| 2.6 | Sandbox режим блокирует побочные эффекты | `side_effects_enabled=False` → `DryRunResult` для write-операций | ✅ Тест `test_sandbox_mode.py` | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Реализовано для `FileTool`, `SQLTool` |

---

## 🏷️ Блок 3: Версионирование и статусы

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 3.1 | Структура `data/` соответствует стандарту | `data/prompts/{type}/{component}/{version}.yaml` | ✅ Скрипт `verify_data_structure.py` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Структура реализована |
| 3.2 | Метаданные содержат статус версии | Каждый файл имеет `status: draft|active|archived` | ✅ Скрипт `verify_prompt_metadata.py` | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Статусы присутствуют |
| 3.3 | `prod` профиль принимает ТОЛЬКО `active` версии | Попытка использовать `draft` в `prod` → ошибка инициализации | ✅ Тест `test_prod_rejects_draft.py` | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Валидация статусов не реализована |
| 3.4 | `sandbox` профиль разрешает `draft` версии | Использование `draft` в `sandbox` → успех | ✅ Тест `test_sandbox_accepts_draft.py` | 🟠 Важный | ❌ | ➡️ | 13.02.2026 | Профили не реализованы |
| 3.5 | Оверрайды версий только в песочнице | `set_prompt_override()` в `prod` → `RuntimeError` | ✅ Тест `test_override_restricted.py` | 🟠 Важный | ❌ | ➡️ | 13.02.2026 | Механизм оверрайдов отсутствует |
| 3.6 | `registry.yaml` содержит активные версии | Единый источник истины для продакшна | ✅ Скрипт `verify_registry.py` | 🟢 Желательный | ❌ | ➡️ | 13.02.2026 | Реестр не реализован |

---

## ⚡ Блок 4: Производительность и эффективность

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 4.1 | 0 обращений к ФС после инициализации | Мониторинг `Path.open()` → 0 вызовов после `initialize()` | ✅ Тест `test_no_fs_after_init.py` | 🔴 Критический | ⚠️ | ➡️ | 13.02.2026 | Частично: промпты предзагружаются, но могут быть обращения к хранилищу |
| 4.2 | Время инициализации агента < 100 мс | Замер `time ctx.initialize()` | ✅ Скрипт `benchmark_init.py` | 🟠 Важный | ❌ | ➡️ | 15.02.2026 | В процессе оптимизации (требуется измерение после последних улучшений) |
| 4.3 | Время горячего переключения < 50 мс | `clone_with_version_override()` < 50 мс | ✅ Тест `test_hot_reload_speed.py` | 🟠 Важный | ❌ | ➡️ | 13.02.2026 | Механизм отсутствует |
| 4.4 | Память на 10 агентов < 1.2 ГБ | `tracemalloc` замер | ✅ Скрипт `benchmark_memory.py` | 🔴 Критический | ❌ | ➡️ | 15.02.2026 | В процессе оптимизации (требуется измерение после последних улучшений) |
| 4.5 | Предзагрузка всех ресурсов при инициализации | Все промпты/контракты загружены до первого шага | ✅ Тест `test_full_preload.py` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Реализовано через `ComponentConfig` |

---

## 🧪 Блок 5: Тестирование и верификация

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 5.1 | Тест изоляции кэшей проходит | `test_cache_isolation.py` → ✅ | ✅ pytest | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Тест падает из-за общего кэша |
| 5.2 | Тест безопасности продакшна проходит | `test_prod_rejects_draft.py` → ✅ | ✅ pytest | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Валидация статусов не реализована |
| 5.3 | Стресс-тест: 50 агентов параллельно | Все агенты завершаются успешно без утечек | ✅ Тест `test_50_parallel_agents.py` | 🟠 Важный | ❌ | ➡️ | 13.02.2026 | Не протестировано |
| 5.4 | Покрытие критических путей ≥ 85% | `pytest --cov=core --cov-report=term-missing` | ✅ pytest-cov | 🟢 Желательный | ✅ | 📈 | 13.02.2026 | ~78% покрытие |
| 5.5 | Все тесты проходят без флапов | 100 запусков подряд → 100% успех | ✅ `pytest --count=100` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Стабильные тесты |

---

## 🧩 Блок 6: Чистота кода и легаси

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 6.1 | Нет `type_provider` в конфигах | Все конфиги используют `provider_type` | ✅ `grep -r "type_provider" config/` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | `dev.yaml.improved` содержит `type_provider` |
| 6.2 | Нет `parameters_schema` в `Capability` | Все валидации через контракты | ✅ `grep -r "parameters_schema" core/` → только в старом коде | 🟠 Важный | ⚠️ | ➡️ | 13.02.2026 | Частично устаревшие поля присутствуют |
| 6.3 | Все компоненты используют `ComponentConfig` | Навыки/инструменты/сервисы имеют `component_config` | ✅ Тест `test_component_config_usage.py` | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Реализовано |
| 6.4 | Нет циклических зависимостей | Граф зависимостей ацикличен | ✅ Скрипт `detect_cycles.py` | 🔴 Критический | ✅ | 📈 | 13.02.2026 | Циклов не обнаружено |
| 6.5 | Нет глобальных синглтонов | Все зависимости через конструктор | ✅ Статический анализ | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Глобальных синглтонов нет |

---

## 📄 Блок 7: Конфигурация (без дублирования)

| № | Проверка | Критерий успеха | Автоматизируема | Приоритет | Статус | Тренд | Дата | Комментарии |
|---|----------|-----------------|-----------------|-----------|--------|-------|------|-------------|
| 7.1 | `InfraConfig` загружается ТОЛЬКО из `core/config/defaults/{profile}.yaml` | Нет других источников для инфраструктурной конфигурации | ✅ `grep -r "InfraConfig\|SystemConfig" core/ \| grep -v "defaults/"` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Конфигурация размазана по коду |
| 7.2 | `AppConfig` загружается ТОЛЬКО из `data/registry.yaml` | Нет `prompt_versions`/`contract_versions` в `dev.yaml` | ✅ `grep -r "prompt_versions\|contract_versions" core/config/defaults/` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | Версии дублируются в `dev.yaml` |
| 7.3 | `AgentConfig` создаётся ТОЛЬКО в коде при запросе | Нет сохранения `AgentConfig` в файлах | ✅ Статический анализ: `AgentConfig` только в `runtime.py`, `main.py` | 🟠 Важный | ✅ | 📈 | 13.02.2026 | Реализовано корректно |
| 7.4 | Нет поля `agent_config` в `dev.yaml` | Полное удаление дублирующего блока | ✅ `grep -r "agent_config" core/config/defaults/` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | `agent_config` присутствует в `dev.yaml` |
| 7.5 | Все провайдеры используют `provider_type` | Нет `type_provider` в конфигах | ✅ `grep -r "type_provider" config/ core/config/` → 0 совпадений | 🔴 Критический | ❌ | ➡️ | 13.02.2026 | `dev.yaml.improved` содержит `type_provider` |
| 7.6 | Чёткое разделение ответственности конфигов | `InfraConfig` → ресурсы, `AppConfig` → поведение, `AgentConfig` → запрос | ✅ Визуальный аудит структуры | 🟠 Важный | ⚠️ | ➡️ | 13.02.2026 | Частично соблюдено |

---

## 📊 Сводная таблица зрелости

| Блок | ✅ | ⚠️ | ❌ | Итог |
|------|----|----|----|------|
| Разделение слоёв | 3 | 2 | 1 | 6/6 (83%) |
| Безопасность и изоляция | 3 | 1 | 2 | 6/6 (75%) |
| Версионирование и статусы | 2 | 0 | 4 | 6/6 (33%) |
| Производительность | 2 | 1 | 2 | 5/5 (60%) |
| Тестирование | 3 | 1 | 1 | 5/5 (80%) |
| Чистота кода | 3 | 1 | 1 | 5/5 (80%) |
| **Конфигурация** | **0** | **1** | **5** | **6/6 (8%)** |
| **Итого** | **16** | **7** | **16** | **39/42 (59%)** |

> ⚠️ **Критическое замечание:** Блок конфигурации имеет **самый низкий уровень зрелости (8%)** — это главный технический долг проекта. Дублирование конфигурации (`agent_config` в `dev.yaml`) нарушает архитектурные принципы и мешает изоляции агентов.

---

## 🎯 Приоритетные задачи на ближайшие 2 недели

| Приоритет | Задача | Блок | Ожидаемый эффект |
|-----------|--------|------|------------------|
| 🔴 1 | Удалить `agent_config` и `prompt_versions` из `dev.yaml` | Блок 7 | ✅ Единственный источник версий — `data/registry.yaml` |
| 🔴 2 | Перенести версии в `data/registry.yaml` | Блок 7 | ✅ Чистое разделение: инфраструктура ≠ поведение |
| 🔴 3 | Исправить `type_provider` → `provider_type` во всех конфигах | Блок 7 | ✅ Корректная регистрация провайдеров |
| 🔴 4 | Перенос регистрации `PromptService`/`ContractService` из `InfrastructureContext` в `ApplicationContext` | Блок 1 | ✅ Изоляция кэшей между агентами |
| 🟠 5 | Реализация валидации статусов версий для профилей `prod`/`sandbox` | Блок 3 | ✅ Безопасность продакшна |

---

## 📈 Целевые метрики на 3 месяца

| Метрика | Текущее | Цель | Срок |
|---------|---------|------|------|
| Общая зрелость | 59% | ≥ 85% | 3 месяца |
| Критические нарушения (🔴) | 9 | 0 | 1 месяц |
| Дублирование конфигурации | Есть | Устранено | 2 недели |
| Время инициализации агента | В процессе оптимизации | ≤ 100 мс | 2 месяца |
| Память на 10 агентов | В процессе оптимизации | ≤ 1.2 ГБ | 2 месяца |
| Покрытие тестами | 78% | ≥ 85% | 3 месяца |

---

## 💡 Как использовать чек-лист

1. **Каждые 2 недели** заполняйте статусы по всем пунктам
2. **Фиксируйте тренд** по сравнению с предыдущей проверкой:
   - 📈 Если улучшилось (было ⚠️ → стало ✅)
   - 📉 Если ухудшилось (было ✅ → стало ⚠️)
   - ➡️ Если стабильно
3. **Обновляйте дату** последней проверки
4. **Добавляйте комментарии** для контекста (особенно при ❌)
5. **Пересчитывайте итоги** в сводной таблице
6. **Формируйте план** на следующие 2 недели на основе приоритетных задач

> 💡 **Совет:** Сохраните этот чек-лист как `ARCHITECTURE_CHECKLIST.md` в корне проекта. Добавьте в `.git/hooks/pre-commit` скрипт, который требует обновления даты проверки при коммитах в `core/`.

---

## 🚨 Красные флаги (остановить развитие и исправить)

| Симптом | Проблема | Действие |
|---------|----------|----------|
| `agent_config` в `dev.yaml` | Дублирование конфигурации | Удалить поле, перенести версии в `data/registry.yaml` |
| `prompt_versions` в `dev.yaml` | Дублирование конфигурации | Удалить поле, перенести версии в `data/registry.yaml` |
| `type_provider` в конфигах | Некорректная регистрация провайдеров | Заменить на `provider_type` + добавить миграцию |
| `id(ctx1.prompt_service) == id(ctx2.prompt_service)` | Общий кэш промптов | Перенести создание `PromptService` из `InfrastructureContext` в `ApplicationContext` |
| Обращения к ФС после `initialize()` | Нет предзагрузки ресурсов | Реализовать кэширование в `PromptService`/`ContractService` |
| `prod` принимает `draft` версии | Отсутствует валидация статусов | Добавить проверку в `ApplicationContext._validate_versions_by_profile()` |

---

## 📌 Почему это критически важно

Дублирование конфигурации (`agent_config` в `dev.yaml`) **блокирует все остальные улучшения**:

| Проблема | Последствие | Решение |
|----------|-------------|---------|
| Версии промптов в `dev.yaml` | Невозможно горячее переключение | Перенос версий в `data/registry.yaml` |
| `agent_config` в инфраструктуре | Смешение ответственностей | Удаление, замена на `AppConfig` |
| `type_provider` вместо `provider_type` | Провайдеры не регистрируются | Исправление конфигурации |

**Без исправления конфигурации изоляция агентов невозможна** — все агенты будут использовать одни и те же версии из `dev.yaml`, а не изолированные `AppConfig`.

---

*Документ обновлён: 15 февраля 2026*
*Следующая проверка: 1 марта 2026*
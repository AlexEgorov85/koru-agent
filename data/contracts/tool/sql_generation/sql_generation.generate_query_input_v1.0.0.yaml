capability: sql_generation.generate_query
component_type: tool
description: Input contract for sql_generation.generate_query
direction: input
schema_data:
  properties:
    natural_language_query:
      description: Запрос пользователя на естественном языке
      type: string
    table_schema:
      description: DDL или описание структуры таблиц
      type: string
    available_scripts:
      description: Примеры SQL запросов в формате few-shot
      type: string
    available_tables:
      description: Список доступных таблиц и колонок
      type: string
  required:
  - natural_language_query
  - table_schema
  type: object
status: active
version: v1.0.0
metadata:
  author: system
  created: '2026-04-15'

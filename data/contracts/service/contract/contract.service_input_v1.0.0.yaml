capability: contract.service
component_type: service
direction: input
status: active
version: v1.0.0
description: >
  Входной контракт сервиса контрактов.
  Запрашивает схему контракта для capability.
schema_data:
  type: object
  properties:
    direction:
      type: string
      description: >
        Направление контракта.
        Значения: "input" или "output".
        По умолчанию "input".
      enum:
        - input
        - output
      default: input
  required: []
capability: json_parsing.parse_to_model
version: v1.0.0
status: active
component_type: service
direction: input
description: Входной контракт для полного цикла извлечения, парсинга и создания Pydantic модели
schema_data:
  type: object
  properties:
    raw_response:
      type: string
      description: Сырой ответ от LLM (может содержать markdown-обёртку)
    schema_def:
      type: object
      description: JSON Schema для валидации и создания Pydantic модели
    model_name:
      type: string
      description: Имя создаваемой Pydantic модели
  required:
    - raw_response

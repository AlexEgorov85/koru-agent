capability: planning.create_plan
component_type: skill
description: Output contract for planning.create_plan
direction: output
schema_data:
  properties:
    plan:
      description: Action plan steps
      items:
        properties:
          action:
            description: Action to take
            type: string
          estimated_time:
            description: Estimated time
            type: string
          step:
            description: Step number
            type: integer
        type: object
      type: array
  required:
  - plan
  type: object
status: active
version: v1.0.0

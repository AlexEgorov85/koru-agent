capability: book_library
component_type: skill
description: Input contract for book_library
direction: input
schema_data:
  properties:
    input:
      description: Input for book_library
      type: string
  required:
  - input
  type: object
status: active
version: v1.0.0

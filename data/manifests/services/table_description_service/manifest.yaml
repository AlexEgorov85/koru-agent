component_id: table_description_service
component_type: service
version: v1.0.0
owner: team-database
status: active

contract:
  input: table_description.get_schema.input.v1.0.0
  output: table_description.get_schema.output.v1.0.0

constraints:
  max_tables_per_request: 20
  timeout_seconds: 30
  cache_ttl_minutes: 60

quality_metrics:
  success_rate_target: 0.99
  avg_execution_time_ms: 200
  error_rate_threshold: 0.01

success_metrics:
  goal_completion_rate: 0.98
  user_satisfaction_score: 4.8
  retry_rate: 0.02

dependencies:
  components: []
  tools:
    - sql_tool
  services:
    - prompt_service

changelog:
  - version: v1.0.0
    date: "2026-02-16"
    author: alexey
    changes:
      - "Initial release"
      - "Table description service with schema introspection"